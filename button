int button1pin = 7;
int button2pin = 8;
void setup() {
    pinMode(button1pin,OUTPUT);
    pinMode(button2pin,OUTPUT);
    Serial.begin(9600);
}
void loop() {
    if(digitalRead(button1pin) == 0){
      Serial.println("off");
    }
}
